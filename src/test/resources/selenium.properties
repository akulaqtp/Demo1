target.host=${selenium.target.host}

# Remote Selenium Server Host (DEFAULT: localhost) 
selenium.server.host=${selenium.server.host}

# Target Browser (DEFAULT: firefox)
target.browser=${selenium.target.browser}